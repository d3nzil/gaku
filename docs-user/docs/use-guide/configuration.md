# Configuration
The configuration screen allows you to tune Gaku to your liking. Currently, there is only a limited set of options.

The configuration screen is available under the `Configuration` section.

Note that when test session related options are changed and tests session is in progress, it is not affected. Only tests (or practice) sessions started after will use the new settings.

